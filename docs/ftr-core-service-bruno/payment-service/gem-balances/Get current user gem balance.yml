info:
  name: Get current user gem balance
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/payments/gems/balances"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
