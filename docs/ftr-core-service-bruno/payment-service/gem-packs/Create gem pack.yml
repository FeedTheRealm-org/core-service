info:
  name: Create gem pack
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/payments/gems/packs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
