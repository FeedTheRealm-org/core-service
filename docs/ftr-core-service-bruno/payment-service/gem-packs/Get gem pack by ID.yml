info:
  name: Get gem pack by ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/payments/gems/packs/:pack_id"
  params:
    - name: pack_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
