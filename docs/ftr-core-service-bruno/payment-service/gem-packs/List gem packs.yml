info:
  name: List gem packs
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/payments/gems/packs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
