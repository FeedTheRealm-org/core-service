info:
  name: Get world player counts
  type: http
  seq: 13
  tags:
    - world-service

http:
  method: GET
  url: "{{baseUrl}}/world/orchestrator/:id/players"
  params:
    - name: id
      value: ""
      type: path
      description: World UUID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/world/orchestrator/:id/players"
      method: GET
      params:
        - name: id
          value: ""
          type: path
          description: World UUID
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/world/orchestrator/:id/players"
      method: GET
      params:
        - name: id
          value: ""
          type: path
          description: World UUID
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 500 Response
    description: Internal Server Error
    request:
      url: "{{baseUrl}}/world/orchestrator/:id/players"
      method: GET
      params:
        - name: id
          value: ""
          type: path
          description: World UUID
    response:
      status: 500
      statusText: Internal Server Error
      body:
        type: text
        data: ""

docs: Returns player counts per zone for a world.
