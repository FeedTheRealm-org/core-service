info:
  name: Get zone per world
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/world/:world_id/zones"
  params:
    - name: world_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
