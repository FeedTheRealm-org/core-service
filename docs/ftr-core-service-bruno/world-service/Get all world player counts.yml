info:
  name: Get all world player counts
  type: http
  seq: 19
  tags:
    - world-service

http:
  method: GET
  url: "{{baseUrl}}/world/orchestrator/players"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/world/orchestrator/players"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
  - name: 500 Response
    description: Internal Server Error
    request:
      url: "{{baseUrl}}/world/orchestrator/players"
      method: GET
    response:
      status: 500
      statusText: Internal Server Error
      body:
        type: text
        data: ""

docs: Returns player counts for all worlds.
