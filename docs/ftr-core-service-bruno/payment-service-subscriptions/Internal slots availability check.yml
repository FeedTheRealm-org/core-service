info:
  name: Internal slots availability check
  type: http
  seq: 3
  tags:
    - payment-service-subscriptions

http:
  method: GET
  url: "{{baseUrl}}/subscriptions/internal/users/:user_id/status"
  params:
    - name: user_id
      value: ""
      type: path
      description: User ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/subscriptions/internal/users/:user_id/status"
      method: GET
      params:
        - name: user_id
          value: ""
          type: path
          description: User ID
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/subscriptions/internal/users/:user_id/status"
      method: GET
      params:
        - name: user_id
          value: ""
          type: path
          description: User ID
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/subscriptions/internal/users/:user_id/status"
      method: GET
      params:
        - name: user_id
          value: ""
          type: path
          description: User ID
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""

docs: Internal endpoint used by world-service to verify available spots.
