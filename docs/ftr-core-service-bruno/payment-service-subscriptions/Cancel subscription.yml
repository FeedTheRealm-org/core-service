info:
  name: Cancel subscription
  type: http
  seq: 1
  tags:
    - payment-service-subscriptions

http:
  method: DELETE
  url: "{{baseUrl}}/subscriptions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/subscriptions"
      method: DELETE
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/subscriptions"
      method: DELETE
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/subscriptions"
      method: DELETE
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""
  - name: 500 Response
    description: Internal Server Error
    request:
      url: "{{baseUrl}}/subscriptions"
      method: DELETE
    response:
      status: 500
      statusText: Internal Server Error
      body:
        type: text
        data: ""

docs: Cancels the user's active subscription if no slots are being used.
