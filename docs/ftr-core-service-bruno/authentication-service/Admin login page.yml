info:
  name: Admin login page
  type: http
  seq: 1
  tags:
    - authentication-service

http:
  method: GET
  url: "{{baseUrl}}/auth"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: HTML form
    request:
      url: "{{baseUrl}}/auth"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
